/node_modules
/dist
.env
# TypeScript incremental build artifact — regenerated by every build, and
# committing it makes the working tree dirty after `npm run build` (which blocks
# `git pull` on the deploy box) and can let a stale entry mask a real rebuild.
tsconfig.tsbuildinfo
